import fs from 'node:fs';const rows=JSON.parse(fs.readFileSync('docs/qa/finance-rpc-dynamic-adapter-review-2026-09-11.json','utf8'));const expected=[...new Set(rows.flatMap(r=>r.resolved_names))].map(name=>({name,keys:['_payload']}));expected.push({name:'update_driver_settlement_km_review',keys:['_settlement_id','_audited_km','_km_status','_notes','_km_start','_km_end','_audited_start_location','_audited_end_location']},{name:'create_manual_driver_settlement',keys:['_tenant_id','_driver_id','_vehicle_id','_reference_date','_load_ids']},{name:'get_finance_unbilled_freight_summary',keys:['_tenant_id','_from','_to','_client_id']},{name:'list_finance_unbilled_freight_origins',keys:['_tenant_id','_from','_to','_client_id','_state','_page']});
const query=`with expected as(select value->>'name' name,array(select jsonb_array_elements_text(value->'keys')) keys from jsonb_array_elements('${JSON.stringify(expected)}'::jsonb)), candidates as(select e.name,e.keys,p.oid,p.proargnames,p.pronargs,p.pronargdefaults,pg_get_function_arguments(p.oid) arguments,case when p.oid is not null then has_function_privilege('authenticated',p.oid,'execute') end auth,case when p.oid is not null then has_function_privilege('anon',p.oid,'execute') end anon from expected e left join pg_proc p on p.pronamespace='public'::regnamespace and p.proname=e.name and p.prokind='f') select name,keys,count(oid) overloads,jsonb_agg(jsonb_build_object('signature',oid::regprocedure::text,'names',proargnames,'arguments',arguments,'named_compatible',keys<@proargnames and proargnames[1:pronargs-pronargdefaults]<@keys,'authenticated',auth,'anon',anon)) candidates from candidates group by name,keys order by name;`;
fs.writeFileSync('docs/qa/finance-rpc-unresolved-catalog-2026-09-11.sql',query);fs.writeFileSync('docs/qa/finance-rpc-unresolved-expected-2026-09-11.json',JSON.stringify(expected,null,2));console.log(expected.length);
