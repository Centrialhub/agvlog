# Revisão financeira 125034 → 132406 — 2026-09-10

Leitura integral das oito migrations financeiras e SHA256 comparado com manifesto atual. Todas coincidem; hashes completos no JSON irmão finance-production-block-125034-132406-hashes-2026-09-10.json. Nenhum apply, execução SQL, PG ou TSC.

## Ordem e conclusão

Ordem financeira: 125034 → 125357 → 130032 → 130540 → 130921 → 130956 → 131149 → 132406.

Não há cron, backfill ou chamada de processamento durante a instalação dessas oito. Nenhuma redefine can_access; conservar gate false e os dois workers pausados. Porém 132406 altera imediatamente o gerador operacional de folha: exige acesso financeiro, portanto geração passa a falhar fechada enquanto gate false. 130540 também instala proteção de pagamentos de acerto vinculados e muda capacidade consumida compartilhada, embora inicialmente sua tabela de links seja vazia.

Não incluir automaticamente as migrations operacionais intercaladas na aplicação deste bloco: 125751 workspace_tenant_foundation, 131125 active_tenant_auth_context, 131419 driver_delivery_receipt_foundation, 132033 enforce_active_tenant_rls e 132149 require_driver_arrival_gps_cutover têm plano/estado próprios. A mera ordenação lexical não prova que é seguro reaplicá-las. A primeira delas contém backfill de workspaces, mas NÃO pertence às oito financeiras aqui revisadas.

## Matriz

| Arquivo | Efeito / dependências | Acesso e condição de instalação fechada |
|---|---|---|
| 20260910125034_finance_manual_expense_cost_center.sql | Substitui record_manual_expense para validar cost_center_id ativo do tenant, preservar ID/nome no evento e nome em payables.cost_center. Mantém obrigação e alocação opcional a movimento existente. Depende de 120756 (writer/evidence), 002244 apply_payable_movement, cost_centers.active/name e payables.cost_center, clients/storage/foundation. | Sem novos grants; CREATE OR REPLACE deve encontrar função anterior para conservar ACL. Writer exige can_access antes e depois lock; gate falso bloqueia sem inserir dados. |
| 20260910125357_finance_recorded_costs.sql | Cria reader consolidando expense_items e manual_expense comprovada por finance_commands, excluindo duplicação por mesmo payable_id. Datas de competência/registro explicitadas. Depende de modelos despesas, payables, comandos e cost_centers. | Helper DEFINER/search_path vazio, can_access; wrapper INVOKER. Revoga todos e concede EXECUTE só authenticated. Sem escrita. Não equivale a caixa nem a histórico completo; posterior 130032 amplia cobertura. |
| 20260910130032_finance_payroll_recorded_costs.sql | Substitui reader para incluir remuneração de folha aprovada/fechada: base_salary/daily/hourly/commission/bonus, natureza credit, valor positivo com duas casas. Outros créditos ficam em contadores de não classificados, sem inclusão nos custos normais. Depende de payroll_entry_items/entries/periods/employees e campos competência/tipo/natureza/status/identidades. | Mantém ACL de 125357. Nenhum cálculo persistido/backfill. Sem gatilho. Projeção ainda não contém hardenings posteriores de cancelamento/valores inválidos: não liberar UI final só com este estado intermediário. |
| 20260910130540_finance_settlement_movement_links.sql | Nova tabela de links de pagamento EXISTENTE integral para saída, índice por movimento, unicidade payment_id, trigger capacidade BEFORE INSERT e imutabilidade. Substitui movement_used_cents incluindo despesas, pagáveis ativos e acertos. Trigger BEFORE UPDATE/DELETE em driver_settlement_payments impede alterar pagamento já vinculado. Writer associa, audita e guarda replay, sem criar dinheiro/pagamento. | Tabela RLS, SELECT authenticated/service_role, nenhuma DML direta. Helpers do comando autenticados via wrapper INVOKER e gate; triggers sem EXECUTE API. movement_used_cents conserva ACL revogada do predecessor (002244/003529): exigir presença/ACL, não criar helper inadvertidamente público. Proteção do pagamento independe de gate se links existirem. |
| 20260910130921_finance_settlement_movement_options.sql | Reader de candidatos: pagamento válido, motorista identificado, mesma data efetiva SP, saída não transferência e capacidade integral disponível. Exibe link/ator/motivo já existente. Depende 130540, driver_settlements/payments, bank_accounts, finance_events e capacity helper. | DEFINER privado + INVOKER público, EXECUTE authenticated, gate falso nega. Não cria vínculo nem altera folha. Esta versão não tem ainda histórico/reversão posteriores, portanto não representa prontidão final da UI atual. |
| 20260910130956_finance_statement_period_evidence.sql | Cria cálculo OFX de declarações/dias/âncoras e aritmética, native_statement_account, bank_entry_active e tabelas import/verificações/entradas. Cria finance_period_evidence_reviews imutável, writer recuperável de revisão e patch audit_events para period_evidence_reviewed. | Tabela RLS, SELECT só authenticated (service_role sem grant explícito); sem DML API. Funções guardadas, wrappers INVOKER. Não fecha período, can_close=false/not_attested. Patch exige needle identity_reviewed_manually,identity_review_reversed no audit_events; aborta se ausente. Nenhuma revisão gerada durante instalação. |
| 20260910131149_finance_settlement_link_audit.sql | Patch de audit_events acrescenta settlement_payment_linked à lista manual. Depende da função audit_events instalada e do mesmo needle preservado por 130956. | Executa somente CREATE OR REPLACE dinâmico do corpo, conservando identidade/ACL. Não insere evento, não muda gate. Reaplicação duplicaria label: usar versão única. |
| 20260910132406_finance_payroll_reimbursement_source_dedup.sql | Cria helper privado deduplicate_payroll_reimbursements e altera public.generate_payroll_period(uuid,date,date,text,boolean,boolean) preservando identidade/defaults/ACL. Acrescenta require_access antes/depois lock financeiro, locks de períodos e verificações de períodos/entradas protegidos e pagamento em período cancelado. Chama dedup antes RETURN _period_id. | Helper sem EXECUTE public/anon/authenticated/service_role; só chamada interna. Guard can_access, período/entries draft ou calculated. A instalação NÃO chama gerador/dedup. Com gate falso, o gerador alterado passa a recusar execução: é efeito operacional imediato, sem desvio pelo owner do helper porque ele também checa acesso. |

## 132406: readiness exata para desbloquear dependente motorista

Pré-requisitos diretos:

- public.generate_payroll_period(uuid,date,date,text,boolean,boolean), PLpgSQL operacional anterior, incluindo wrapper financeiro esperado; pg_get_functiondef deve conter exatamente a âncora de duas linhas “  SELECT id INTO _period_id” + “  FROM public.payroll_periods” e “  RETURN _period_id;”, e ainda NÃO conter deduplicate_payroll_reimbursements.
- finance_private.require_access(uuid), can_access(uuid) e view privada active_payable_payments criada em 003529. Essa view é necessária mesmo com check de paid periods cancelados, e deve permanecer sem SELECT API.
- payroll_periods/entries/items com campos status, driver/employee, source_table/source_id/source_metadata, natureza/tipo/amount/locked; payables.source_table/source_id; driver_settlements com needs_recalculation, status, driver_payable_amount/final_amount e driver_reimbursement_total; driver_settlement_items com metadata/source; driver_expenses e recompute_payroll_entry_totals(uuid).

O helper identifica despesas por UUID de origem e compara metadados aprovados/reembolsáveis, soma e unicidade. Em geração futura de folha editável, remove somente item de reembolso avulso não bloqueado cuja fonte/valor correspondem ao já contido no crédito do acerto, preserva cobertura reimbursement_sources no crédito e recalcula totais. Fontes históricas ambíguas ou já usadas por outra folha ativa/paga causam revisão, sem deduzir por igualdade casual de valor/data. Não corrige histórico preexistente no apply.

A criação do helper desbloqueia a aplicação posterior que lê/patcha sua definição (referência concreta: 20260910134948_finance_canonical_trip_cost_settlement.sql:92). NÃO desbloqueia funcionalmente folha/motorista enquanto gate false. Essa versão entende despesas de acerto com source_table driver_expenses; a extensão posterior para finance_expense_items é necessária antes de operar com custos canônicos no acerto. Não modificar 132406 para antecipar essa regra nem abrir gate para “testar” o helper.

Conferência posterior de instalação: helper existe com retorno void, SECURITY DEFINER/search_path vazio, sem EXECUTE API; gerador contém uma chamada deduplicate_payroll_reimbursements antes RETURN e guards financeiros; OID/defaults/ACL do gerador preservados. Registrar hash atual do helper para a frente dependente. Não invocar geração como verificação read-only.

## Pontos comuns de preflight

Confirmar predecessores antes dos CREATE OR REPLACE 125034,130032 e movement_used_cents em130540: se ausentes, a instrução criaria função com privilégios default em vez de conservar ACL. Nenhum desses atalhos deve substituir a cadeia anterior.

Confirmar duas novas tabelas (settlement_movement_links e period_evidence_reviews) ainda ausentes antes apply; depois verificar RLS/grants e triggers. Políticas de tenant ativo aplicadas em lote anteriormente não aparecem automaticamente em novas tabelas; essas policies chamam can_access e dependem do hardening final de workspace planejado pelo coordenador. Durante staging, constant false mantém leitura autenticada negada.

Os readers/funções novos não comprovam que a interface final já funciona, pois existem extensões posteriores de reversão, cancelamento, origem e fechamento. Esta revisão não executou banco nem validou dados/catalog remoto.
