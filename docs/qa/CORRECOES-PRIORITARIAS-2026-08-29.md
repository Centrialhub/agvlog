# Correções prioritárias — execução e evidências

Estado: **em andamento; não liberado como 100% pronto para produção**.
Referência temporal: 29 a 31/08/2026 em São Paulo; versões de migração usam horário UTC.

## Plano de conclusão e condição de custo

Checkpoint de produção de 31/08: o P1 carga↔viagem foi publicado nas versões
20260831230903 e 20260831230957. A carga 1003 foi reconciliada para ready, a
viagem permaneceu planned sem horário inventado e a contagem global de
violações ficou em zero. O ensaio autenticado de início e replay passou em
transação revertida. A ocorrência 0cff2aa3-... teve também o load_id removido
e auditado pela versão 20260831232156; a RPC agora deixa parada, cliente,
carga e documento nulos quando a seleção é vazia. A chegada GPS foi instalada
aditivamente em 20260831232458, com 500 m e precisão máxima de 150 m, mantendo
temporariamente a assinatura legada. O commit Lovable contém a chamada de
quatro argumentos e o deploy foi solicitado sem créditos de IA, porém a URL
pública ainda servia o chunk anterior, que chamava a assinatura de um
argumento. Por isso o cutover permanece suspenso. Todos os smokes de banco
terminaram em rollback; nenhuma emissão fiscal, pagamento, SSX ou provedor
externo foi acionado. Evidência consolidada:
[P1/P2 em produção](P1-P2-PRODUCAO-2026-08-31.md).

Checkpoint local do operador de 31/08: ocorrências do motorista agora aparecem
no detalhe da viagem na Torre e no detalhe da carga; erros de leitura não são
mais convertidos em falso estado vazio/inexistente e a transição para
in_transit fica bloqueada até confirmar o vínculo. Foram 21 de 21 testes
focados aprovados, além de regressão da Torre, lint e whitespace. Publicação
permanece pendente. Evidência:
[operador e ocorrências](OPERADOR-OCORRENCIAS-2026-08-31.md).

Checkpoint mais recente de 31/08: [rotas recuperáveis e estimativa financeira](TORRE-ROTAS-RECUPERAVEIS-2026-08-31.md). Corrigidos GPS antigo, omissão de paradas e gravação após mudança de contexto; preparação/commit serializados, replay durável, timeout com recuperação e geometria vinculada ao plano atual. O trecho restante não substitui mais a estimativa completa no acerto; pagamentos preservados e quilometragem desconhecida explicitada na tela. **Gate final: 2.619 testes/221 arquivos**, tipos/lint/qualidade/42 sintaxes Edge/build/scanner aprovados; **313 cenários PostgreSQL nativos aprovados** e servidor descartável parado. Contenção/retomada preservaram hashes de rotas e recibos. Hash de 1.064 arquivos igual antes/depois. **Nada publicado neste bloco.** Consulta agregada confirmou SSX efetivo em zero tenants e ausência das novas APIs/colunas em produção. Próximo: ingestão monotônica e encadeamento SSX inativo; demais P1/P2, release coordenado e E2E autenticado do motorista; depois prontidão integral do operador. Docker CLI e configuração local de testes autenticados continuam indisponíveis. O gate anterior de 2.616 testes precedeu apenas o ajuste final de timeout; tentativas encerradas em erros de tipo/lint não foram contadas como aprovação.

Checkpoint de 31/08: [Torre de Controle — avaliação transacional](TORRE-CONTROLE-TRANSACOES-2026-08-31.md). Status e alertas calculados/gravados juntos no banco, replay sem duplicação, geometria por segmentos e invalidação de métricas/alertas por mudança de contexto. A Edge usa JWT do usuário; função pública INVOKER e implementação privada com papel/MFA/tenant/capacidade revalidados após locks. **27 testes novos; 2.558 testes/216 arquivos aprovados**, tipos/lint/qualidade/42 sintaxes Edge/build/scanner aprovados; **301 cenários PostgreSQL nativos aprovados** e servidor descartável parado. Hash de 1.055 arquivos idêntico antes/depois. **Nada publicado.** Produção ainda sem a nova API/schema e com leitores legados; SSX efetivamente habilitado em zero tenants. Próximo: rotas, ingestão monotônica e encadeamento SSX inativo, demais P1/P2 e liberação autenticada do motorista. **Após o motorista, concluir todos os fluxos do operador para produção**, conforme objetivo ampliado; nenhum desses critérios foi reduzido aos testes locais. A primeira execução geral foi interrompida, sem aprovação; o resultado citado é da execução posterior concluída.

Checkpoint anterior: [Torre de Controle — contratos de leitura e ações explícitas](TORRE-CONTROLE-CONTRATOS-2026-08-30.md). Corrigidos localmente os leitores que excluíam `in_transit`, a revogação incompatível das duas APIs, os erros silenciosos da tela e os falsos sucessos de cálculo. Leituras agora usam RLS/SECURITY INVOKER; ações Edge usam JWT/papel/MFA e reavaliação bloqueada com SSX desligado. **46 testes novos; 76 focados aprovados. Gate final: 2.531 testes/214 arquivos**, tipos, lint, qualidade, 42 sintaxes Edge, build e scanner aprovados; hash de 1.051 arquivos idêntico antes/depois. O primeiro gate teve uma única falha em expectativa antiga que exigia 30 handlers administrativos e ausência de MFA; o teste agora controla inventário explícito e JWT/MFA da Torre. Metadados de produção confirmaram os dois corpos legados e ausência de `row_to_jsonb`; zero tenants com SSX efetivamente habilitado. **Nada publicado neste bloco.** Próximo: transação/revisão de status, alertas e rotas, ingestão monotônica e encadeamento automático SSX, mantendo-o inativo; demais P1/P2, release coordenado e E2E autenticado continuam pendentes.

Todas as cargas existentes estão autorizadas para teste e alterações de produção estão autorizadas **sem gastos extras**. O objetivo ampliado proíbe **qualquer emissão fiscal durante manipulações e QA**. Não ativar serviço pago, aumentar plano, acionar pagamentos externos ou reintegrar SSX para testar. A autorização permite manipulações e testes internos; não confirma gratuidade de provedores, mensagens, scanner externo ou rastreamento. Usar fixtures e testes reais delimitados com efeitos externos controlados. Não inventar horários históricos para reconciliar cargas.

| Prioridade | Trabalho de conclusão | Critério de aceite |
| --- | --- | --- |
| P1 — privacidade da ocorrência | Manter seleção vazia sem associação automática; publicar UI compatível e revisar leitores/arquivos do portal | Ocorrência interna não aparece ao cliente; outro tenant/nota é negado; escopo explícito mantém apenas os vínculos escolhidos |
| P1 — carga/viagem/entrega | Concluir reentrega auditada, cortar escritores diretos e publicar correção/transições atômicas coordenadas | Partida/baixa atualizam todo o grafo ou nada; replay e concorrência não duplicam resultados; nova tentativa preserva a anterior e o financeiro |
| P1 — convite | Alinhar configuração do Auth hospedado com a política e manter provisionamento restrito | Cadastro público e criação arbitrária de tenant negados; convite válido, login e recuperação continuam funcionando |
| P1 — privilégios | Revisar cada função por chamada, papel, tenant, search_path e necessidade de SECURITY DEFINER; confirmar tabelas backend-only | Matriz de permissões testada por papel/tenant; revogações individuais sem quebrar consumidores; proteção de senhas habilitada somente se disponível sem custo adicional |
| P1 — fiscal | Concluir seleção exata de ambiente/credencial, idempotência e tratamento de resposta incerta; validar pré-requisitos locais por tipo de documento | QA não usa produção nem fallback global; replay não reemite; rejeições são classificadas e corrigidas antes de retransmissão; unicidade considera tenant/CNPJ/código normalizados e o modelo real de filiais |
| P2 — chegada | Coordenadas recentes, precisão, geofence configurável e exceção operacional justificada | Chamada direta sem dados válidos é negada; fora da área exige fluxo de exceção; testes PostGIS e navegador real. GPS não é tratado como prova antifraude absoluta |
| P2 — jornada | Finalizar UI/concorrência da máquina de estados já publicada | Sequências inválidas/duplicadas são negadas; checklist por turno e troca de viagem preservam jornada; duração anormal não é confundida com regra temporal inventada |
| P2 — despesas | Publicar labels/nomes/zoom e verificar o fluxo completo de despesa/anexo/aprovação | Axe sem os achados críticos reportados; teclado e zoom funcionam; valor e anexo chegam corretamente à operação sem duplicação |
| P2 — eventos | Publicar tradução de tipos e descrição/contexto seguros na lista | Lista e detalhe apresentam o mesmo evento compreensível, sem expor dados internos no portal |

Em cada publicação: preflight de versão/permissões, contenção ou recuperação ensaiada, teste backend e frontend, regressão dos módulos interligados e smoke autenticado. Ordem de banco/Edge/frontend deve manter compatibilidade; nenhuma árvore local é promovida apenas porque o build passou. A restrição anterior aos triggers amplos não será contornada.

Bloco concluído localmente: [notificações por contexto de acesso](NOTIFICACOES-CONTEXTO-2026-08-30.md). Avisos, histórico interno e confirmações ficam vinculados à conta/empresa/papel; respostas e callbacks antigos não reaparecem no contexto seguinte. **32 testes novos aprovados**, incluindo 18 integrações frontend/SDK/SQL. O mock financeiro desatualizado foi removido e seus 24 testes passaram com o hook real. **Gate final: 2.485 testes/209 arquivos**, tipos, lint, qualidade, 41 sintaxes Edge, build e scanner aprovados; hash agregado de 1.041 arquivos idêntico antes/depois. Maior chunk 488,3 KiB. **Não publicado.** A [coordenação MFA](MFA-SESSAO-2026-08-30.md) também permanece corrigida/testada localmente, com restauração de banco e frontend ainda dependente de publicação coordenada. Os 290 ensaios PostgreSQL nativos pertencem ao checkpoint anterior; não foram repetidos, pois este bloco não alterou migrações. Próximo: reconciliar o rascunho de privilégios com as APIs ainda consumidas pela Torre de Controle e validar o contrato de viagens/rastreamento com SSX inativo. Auth hospedado, fiscal, GPS, anexos, demais P1/P2, publicação e E2E autenticado continuam pendentes. Nenhuma escrita em produção nem serviço pago neste bloco.

Checkpoint anterior: [sessão, tenant e listagens de acertos](SESSAO-TENANT-ACERTOS-2026-08-30.md). Corridas de sessão, cache entre contas/empresas, consultas financeiras após erro e confirmações globais antigas foram reproduzidas e corrigidas localmente. **39 testes novos de frontend/SQL aprovados** e regressão de **290 cenários PostgreSQL 17.11** aprovada, cluster parado. Gate amplo final: **2.399 testes/203 arquivos**, tipos, lint, qualidade, 41 sintaxes Edge, build e scanner aprovados, código zero. **Não publicado.** Auth hospedado foi reconsultado e continua com `disable_signup=false`; a autenticação administrativa da CLI foi solicitada, e o navegador suportado falhou antes de conectar. Advisors naquele checkpoint: 140 funções privilegiadas, um aviso de senha vazada e três avisos informativos RLS; catálogo confirmou as três tabelas fechadas para anon/authenticated. Contagens agregadas dos dois acertos existentes não apontaram incompatibilidade nos campos verificados. Nada escrito em produção, nenhum gasto extra contratado e SSX inativo.

Checkpoint anterior de ajustes: [ajustes auditados dos acertos](ACERTOS-AJUSTES-AUDITADOS-2026-08-30.md). Inclusão/remoção corrigidas para acertos manuais e de viagem, valores estritos em centavos, histórico preservado, replay idempotente, MFA e revisão esperada. A tela real usa recuperação durável, labels e consulta do detalhe isolada por empresa/ator. **Não publicado.** Gate final: **2.360 testes/200 arquivos**, tipos, lint, qualidade, 41 sintaxes Edge, build e inspeção pública aprovados, código zero. **290 cenários PostgreSQL 17.11 nativos aprovados**, incluindo nove novos; cluster parado. Contenção/retomada verificam 18 funções e preservam as guardas nos catálogos PostgreSQL 17/18. Restam listagem/sessão e fluxo financeiro integral, Auth hospedado, privilégios, fiscal, GPS, anexos, demais P1/P2, publicação coordenada e E2E autenticado. SSX continua inativo e nenhum serviço pago foi ativado.

Checkpoint anterior de MFA: [MFA na criação de despesas](DESPESAS-MFA-2026-08-30.md), após [chat por ocorrência](CHAT-OCORRENCIAS-2026-08-30.md) e [chat direto](CHAT-MOTORISTA-OPERACAO-2026-08-30.md). Owner/admin precisam de AAL2 na criação, consulta, comprovantes, replay e recálculo; sete wrappers públicos são INVOKER e a autorização é refeita após locks. O gateway de comprovantes usa o JWT do usuário. Rascunhos e confirmações incertas são preservados. **Nada destes blocos foi publicado.** São 34 testes novos de MFA e **281 cenários PostgreSQL nativos aprovados**, processo encerrado com código zero e cluster parado. Gate final aprovado: **2.301 testes/195 arquivos**, tipos, lint, qualidade, 41 sintaxes Edge, build e scanner de artefatos, código zero. A repetição com dois workers resolveu os timeouts de inicialização observados no primeiro ensaio amplo, sem retirar testes ou aumentar seus limites. Build em 19,84 s, maior chunk 488,3 KiB. O gate anterior de chat aprovou 2.267 testes/190 arquivos. Migrações anteriores preservadas e contenção/retomada MFA ensaiada. Restam anexos, reconciliações, auditoria das demais funções, Auth hospedado, fiscal, GPS, demais P1/P2, publicação coordenada e E2E autenticado. Advisors locais indisponíveis por ausência da stack em 127.0.0.1:54322; navegador sem E2E comprovado. Os números das seções antigas são históricos.

Validação local mais recente (etapa C, sem novo deploy): **1.846 testes em 150 arquivos e 193 ensaios PostgreSQL nativos aprovados**, além de tipos, lint, sintaxe Edge, build e scanner. Processos encerrados com código zero e cluster temporário parado. O timeout de uma fixture integrada foi ajustado de 5 para 15 segundos após reprodução isolada bem-sucedida, sem remover asserções ou alterar limites do aplicativo. Os detalhes e pendências estão em [conferência de notas](CONFERENCIA-NOTAS-2026-08-30.md).

## Aplicado e verificado no banco de produção

| Correção | Evidência | Limite |
| --- | --- | --- |
| Ocorrência `0cff2aa3-2aca-431d-ad7d-26367b6f48c2` tornada interna e desvinculada do cliente, parada e nota fiscal | Leitura posterior confirmou `visible_to_client=false`, `client_action_required=false` e vínculos nulos; auditoria `repair_driver_occurrence_scope` preserva os dados anteriores | Não foi comprovado se houve visualização histórica pelo cliente |
| RPC `driver_create_operational_occurrence` não escolhe parada quando o motorista deixa a seleção vazia | Migração `20260830013356_harden_driver_occurrence_scope`; chamada com papel `authenticated` e identidade de motorista, dentro de transação revertida, confirmou escopo da viagem e visibilidade interna | Nenhuma ocorrência de teste permaneceu no banco; frontend atualizado ainda não publicado |
| Criação de tenants restrita à administração controlada | Migração `20260830013726_restrict_tenant_creation_to_platform`; `has_function_privilege` falso para `anon`, `authenticated` e `service_role` | Configuração de signup do Auth hospedado ainda precisa ser alinhada; não é uma liberação integral da política de convites |
| Jornada pessoal contínua e checklists novos por turno | Migração `20260830024309_enforce_driver_journey_state_machine`; testes autenticados de novo turno, sequência, checklist, replay e versão esperada passaram em transação revertida | Frontend ainda local; concorrência com sobreposição comprovada e E2E autenticado continuam pendentes |
| Bloqueio da RPC legada que contornava a jornada | Migração `20260830033306_restrict_legacy_driver_event_api`; `driver_report_event_v1` negada a `authenticated`/`anon`, acesso de serviço preservado | Restrição pontual; não equivale à revisão integral das demais funções privilegiadas |
| Saída exige chegada e não duplica eventos | Migração `20260830042313_harden_driver_stop_departure`; teste pós-deploy com motorista atribuído confirmou validação, replay único, auditoria e rejeição de acesso indevido | Saída não conclui entrega; ajustes da tela ainda locais. Ver [verificação da saída](DEPARTURE-VERIFICATION-2026-08-30.md) |
| APIs de entrega instaladas em etapa privada | Migração `20260830050226_enforce_delivery_outcome_atomicity`; papéis API negados, ensaio revertido com 103 notas e acerto financeiro pendente validado, hashes restaurados | Ainda não disponíveis ao motorista; corte legado e frontend pendentes. Ver [rollout de entregas](DELIVERY-ROLLOUT-VERIFICATION-2026-08-30.md) |
| Leitura cruzada da tabela de idempotência corrigida | Migração `20260830061800_restrict_idempotency_key_read_scope`; reprodução com chaves sintéticas e operador de outro tenant, seguida de isolamento confirmado no pós-deploy | Zero chaves reais preexistentes; não foi comprovada leitura histórica por terceiros. Corpos/ACLs dos consumidores e replay preservados. Ver [evidências RLS](IDEMPOTENCY-RLS-VERIFICATION-2026-08-30.md) |
| Detalhe do portal não publica notas internas nem ocorrências de outra nota/cliente na mesma parada | Migração `20260830115234_harden_portal_shipment_detail_privacy`; duas RPCs de leitura, hashes/ACLs confirmados e seis cenários pós-deploy em transação revertida | Zero acessos ativos no momento da conferência; não há comprovação de acesso indevido histórico. Frontend e auditoria dos demais endpoints pendentes. Ver [privacidade do portal](PORTAL-PRIVACY-VERIFICATION-2026-08-30.md) |

Assessores após o ajuste RLS de 30/08/2026 06:18 UTC: **140** alertas de funções `SECURITY DEFINER` executáveis por `authenticated` (141 após jornada), três tabelas com RLS sem políticas e um alerta de proteção contra senhas vazadas. A redução decorre da restrição individual de `driver_report_event_v1`; a correção RLS não adicionou alertas. A API intencional `driver_get_journey_context` verifica motorista ativo/tenant e retorna apenas eventos do próprio ator. Helpers continuam sem execução por `authenticated`/`anon`. Não houve revogação indiscriminada. As três tabelas aparentemente são de backend e exigem confirmação do fluxo antes de qualquer alteração de políticas. [Orientação para revisão dos privilégios](https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable).

## Implementado localmente; publicação ainda pendente

- Ocorrências: seleção vazia preservada no frontend; listagem traduz tipos e inclui descrição/contexto.
- Portal: detalhe valida ator/empresa/documento retornados, separa cache e não troca para a API legada após negação ou falha de rede. 13 testes da página real integrada ao SQL local passaram; backend de privacidade já publicado, frontend não. Comprovantes históricos tratados no lote local abaixo; correção/reentrega e proteção dos arquivos permanecem pendentes.
- Despacho: candidata transacional com elegibilidade e cobertura documental completa; reenvio durável nas duas telas, rascunhos com atualização condicional e horário com fuso explícito. Passaram 13 novos casos nativos de concorrência, testes das telas reais e frontend/candidata SQL. Ainda faltam composição/replanejamento, fluxo de itens manuais, recuperação específica e ensaio autenticado. Ver [despacho recuperável](DESPACHO-RECUPERAVEL-2026-08-30.md).
- Composição/realocação: oito defeitos do legado reproduzidos localmente; candidata protege isolamento, totais dos dois lados, seleção integral, mercadoria manual remanescente e parada na mesma viagem não iniciada. Tela exige confirmação completa e não apaga carga por conta própria. Passaram 35 testes SQL, 36 frontend/integração e 15 novos ensaios nativos, incluindo recuperação. Inclusão/remoção após planejamento foi tratada no lote abaixo; proteção de todos os escritores e fluxo manual completo **permanecem pendentes**. Ver [composição de cargas](COMPOSICAO-CARGAS-2026-08-30.md).
- Replanejamento entre viagens: candidata explícita com destino/revisão/motivo, atualização atômica de itens e paradas, preservação de histórico e reenvio durável na tela real. Passaram 58 testes específicos e 23 novos nativos, incluindo replanejar → iniciar → entregar → acerto pendente, zero pagamentos. Recuperação recusa uso prévio ou concorrente; antes do primeiro uso, restauração/reaplicação preservou as rotas. Não publicado. Ver [replanejamento e recuperação](REPLANEJAMENTO-CARGAS-2026-08-30.md).
- Inclusão/remoção documental: candidata atômica com parada explícita, seleção integral, proteção de evidências e replay mesmo após remoção da carga vazia. Tela real e painel global recuperam a requisição persistida; filtro com erro de inicialização corrigido. Passaram 70 testes específicos e 26 novos nativos. Recuperação recusa uso e concorrência, inclusive wrappers legados; recuperação fora de ordem bloqueada. Upsert tratado no lote abaixo; DML direto, baixa/reversão/reentrega em `LoadNotesPanel` e demais consumidores ainda pendentes. **Não publicado.** Ver [documentos e recuperação](ALTERACAO-DOCUMENTOS-CARGA-2026-08-30.md).
- Preparação de itens: upsert protege identidade fiscal, valores válidos, evidências e ordem dos locks; API nova exige valores anteriores e requisição recuperável. Painel real e ingestão usam a fila persistente; falha de pedido não vira sucesso silencioso nem duplica item após resposta perdida. Passaram 105 testes novos e 22 novos ensaios nativos. Recuperação antes do uso restaura corpo/ACL; depois do uso, ou fora de ordem, é recusada. API legada compatível, exclusão manual, isolamento das consultas, ingestão integral e mercadoria manual até parada/comprovante continuam pendentes. **Não publicado.** Ver [preparação de itens](PREPARACAO-ITENS-2026-08-30.md).
- Baixa operacional por nota: confirmação explícita com revisão, motivo/horário, comprovante manual pendente e histórico imutável. Fila recupera a mesma solicitação após perda de resposta. Motorista conclui somente notas restantes, sem alterar resultados anteriores; totais de devolução e atualização do detalhe do portal corrigidos. Passaram 77 novos testes e 14 novos ensaios nativos. Recuperação compatível com catálogos PostgreSQL 17/18 mantém guardas de schema, uso e concorrência. **Correção/reentrega e escritores legados continuam pendentes; não publicado.** Ver [baixa operacional e limites](BAIXA-OPERACIONAL-POR-NOTA-2026-08-30.md).
- Comprovantes versionados: candidata preserva IDs e dados históricos, cria uma versão atual separada e ajusta 16 leitores. Portal diferencia comprovantes atuais/anteriores e verifica autorização de download; confirmação do motorista atualiza caches interligados sem falso erro de commit. Contenção suspende os dois escritores sem apagar evidências. Correção foi acrescentada no lote seguinte; **reentrega e preservação de arquivos no Storage não concluídas. Ainda local.** Ver [versionamento e testes](VERSIONAMENTO-COMPROVANTES-2026-08-30.md).
- Correção auditada: API transacional exige resultado anterior/revisão/motivo e preserva histórico, comprovantes e pagamentos existentes; interface usa recuperação v2 e substitui a desmarcação direta. Guard no commit impede reset legado dos resultados registrados. Acertos ficam sinalizados para revisão, com pagamento/aprovação/fechamento bloqueados; contenção específica mantém dados e leitores. **Não publicado; não libera nota para reentrega e não encerra os escritores diretos restantes.** Ver [correção e validação financeira](CORRECAO-AUDITADA-ENTREGAS-2026-08-30.md).
- Carga/viagem: deadlock real reproduzido e corrigido na candidata; locks alinhados com entrega, espelhos de UPDATE corrigidos e recálculo dos dois lados da reatribuição. Recuperação guardada ensaiada com histórico e acerto financeiro preservados. Frontend trata conflitos/respostas incertas e impede início duplicado. Migração ainda não aplicada; não há reparo automático de registros antigos. Ver [concorrência e recuperação](CONCORRENCIA-CARGA-VIAGEM-2026-08-30.md).
- Chegada: captura compartilhada de GPS no frontend e migração exigindo coordenadas/precisão e proximidade no backend; faltam testes completos com PostGIS, exceção operacional auditada e estratégia de compatibilidade do deploy.
- Jornada (frontend pendente): estado por motorista/tenant entre viagens, novo turno após encerramento e checklist com corte temporal por turno. Rascunhos preservados em rerender, falhas de consulta e atualização remota; revisão esperada impede sobrescrita silenciosa. O banco correspondente já foi publicado e testado. Falta validar seleção/troca de contexto em todos os cenários, inclusive reatribuição da viagem durante uma jornada aberta.
- Entregas/comunicações: etapa aditiva **publicada mas privada**, preservando cinco contratos legados. Corte/recovery separados e ensaiados; helpers sem privilégios elevados, APIs novas só serão liberadas junto com o corte. 69 testes de banco, 19 de rollout, 12 de efeitos fiscais/financeiros, 42 de envio/tela e oito nativos multissessão. Upload real, fila durável, respostas/anexos e release compatível ainda pendentes. Ver [rollout e evidências](DELIVERY-ROLLOUT-VERIFICATION-2026-08-30.md).
- Despesas: nomes acessíveis, associação de labels e zoom liberado; falta repetir Axe e fluxo real no navegador.
- Fiscal: ambiente explícito (`sandbox`, `homologation`, `production`), seleção exata de credencial e remoção do token global como fallback no Hub/polling; emissão inicia em homologação nas telas. Falta concluir idempotência, reconciliação de resultados incertos e validação dos caminhos alternativos de arquivos.
- SSX: operação permanece desativada por capacidade; lógica e checklist de reativação preparados sem ativar serviço ou gerar cobrança.

## Testes concluídos nesta etapa

Gate `npm run check` com Node **22.23.2** e npm **10.9.4**: **aprovado**.

- Gate mais recente, lote local de correção auditada: **1.626 testes/140 arquivos aprovados**, além de **166 ensaios PostgreSQL nativos** em execução separada. São 76 novos testes e 16 novos nativos em relação a comprovantes; ambos os processos encerraram com sucesso e o cluster temporário foi parado. Lote de comprovantes: 1.550/136 e 150 nativos; privacidade do portal: 1.501/132 e 141; baixa operacional: 1.455/128 e 133; preparação: 1.378/123 e 119; documentos: 1.273/116 e 97; replanejamento: 1.203/111 e 71; composição: 1.145/107 e 48; despacho: 1.074/102 e 33. As evidências locais mais novas não significam publicação do frontend ou das migrações pendentes.
- Último `npm audit --audit-level=high`: nenhuma vulnerabilidade reportada; não repetido neste lote, que não adicionou dependências de produção.
- TypeScript, lint de erros, lint dos módulos críticos e baseline de qualidade aprovados.
- Sintaxe das 40 Edge Functions aprovada; isso não equivale a typecheck completo do runtime Deno.
- Build de produção, orçamento de bundle (maior chunk 488,3 KiB; limite 500 KiB) e inspeção de artefato público aprovados.
- O gate inclui cobertura do subconjunto configurado, não do aplicativo inteiro. No lote de privacidade do portal: 93,03% linhas/statements, 65,83% branches e 81,81% funções; não extrapolar essa medição para prontidão integral.
- Testes PGlite executam PostgreSQL/PLpgSQL em esquema mínimo, incluindo jornada, saída, despacho, composição/replanejamento, documentos, preparação, baixa por nota, comprovantes, correções e autorização de ocorrências/tenants/portal. Os 166 ensaios nativos incluem sessões reais, integridade no commit, contenção/restauração e fixtures operacionais/financeiras, mas não a pilha Supabase completa, RLS integrada e PostGIS. Os casos novos usam baseline local e dados sintéticos, sem nova exportação de definições privadas de produção.
- Testes do handler fiscal usam o código real da Edge Function com banco e provedor simulados; não emitem documentos fiscais e não chamam serviços externos.

## Critérios ainda necessários para liberação

Revisão local posterior de privilégios encontrou uma incompatibilidade no rascunho `20260830005603_close_authenticated_security_definer_surface.sql`: `get_active_trips_live(uuid)` e `get_open_trip_alerts(uuid)` ainda são chamadas por `useActiveTripsLive.ts`, consumido pela rota `/operations-control`. Essas duas revogações foram removidas da candidata e os leitores foram corrigidos/testados com RLS/SECURITY INVOKER no [checkpoint da Torre](TORRE-CONTROLE-CONTRATOS-2026-08-30.md), ainda **sem publicação**. Hashes dos corpos em produção coincidem com a baseline reproduzida, e o catálogo confirma a ausência de `row_to_jsonb`. Isso não conclui a auditoria individual das outras assinaturas nem a preparação integral da reintegração SSX.

1. Concluir regras operacionais e reparos com auditoria sem inventar horários de início ou confirmar entrega não realizada.
2. Executar migrações e pgTAP na pilha Supabase isolada completa. PostgreSQL nativo portátil agora está disponível para fixtures/concorrência; isso não equivale à pilha completa com Auth, Storage, RLS e PostGIS.
3. Testar ponta a ponta motorista → banco → operação → portal, incluindo reenvio, falha de rede, concorrência e isolamento entre tenants.
4. Concluir proteção contra duplicação fiscal e validar homologação com credenciais próprias, sem transmissão em produção.
5. Revisar individualmente privilégios, signup hospedado e configurações fiscais; publicar frontend/Edge/RPC em ordem compatível e repetir smoke pós-deploy.

## Correção de premissa da auditoria inicial

Os dois emitentes ativos com código `MATRIZ` observados no banco tinham CNPJs distintos. Não foram mesclados ou excluídos. O código de filial repetido e a necessidade de unicidade precisam considerar o modelo de múltiplos emitentes, sem presumir duplicação da pessoa jurídica.

Nenhum serviço pago adicional foi ativado e nenhuma emissão fiscal real foi feita durante estes testes.

Revisão posterior à publicação da saída (30/08/2026 04:23 UTC): assessores mantiveram 140 alertas de funções privilegiadas, três RLS sem políticas e proteção contra senhas vazadas pendente. Detalhes, recuperação e limites em [verificação da saída](DEPARTURE-VERIFICATION-2026-08-30.md).

## Atualização — testes com cargas existentes e bloqueio de publicação

O usuário autorizou explicitamente o uso de todas as cargas existentes para testes. A carga 1003 foi usada em transações revertidas contra as **RPCs atualmente publicadas**, sob papel `authenticated` e identidade do motorista atribuído:

- Início seguido de repetição: carga e viagem em trânsito, horário registrado, motorista e veículo sincronizados e apenas um novo evento `trip_started` dentro da transação.
- Carga temporariamente em hold: início recusado; viagem permaneceu planejada, sem horário de partida.
- Após os rollbacks: consulta independente confirmou os valores anteriores de status, início, hold e `updated_at`. Nenhuma partida de teste ficou gravada.

Isso valida a linha de base, **não** a nova migração em produção. A revisão automática rejeitou `enforce_trip_load_transit_invariant` por adicionar triggers amplos e substituir a RPC de início sem ensaio Supabase completo e plano de recuperação demonstrado. Não houve nova tentativa por outra ferramenta. Consulta posterior confirmou ausência dos três helpers novos e manutenção das definições anteriores.

A aprovação específica foi solicitada para publicação após validação isolada; liberar cargas para teste não foi tratado como aprovação automática desse deploy. Posteriormente o usuário autorizou explicitamente qualquer alteração em produção sem gastos extras. Essa autorização foi usada para o lote delimitado de jornada, não para contornar a rejeição anterior dos triggers de carga/viagem. Ver [ensaio e recuperação](ENSAIO-CARGA-VIAGEM-2026-08-30.md).

## Atualização — lote de jornada publicado em 30/08/2026 02:43 UTC

- Preflight: três definições/permissões capturadas em `JOURNEY-PREDEPLOYMENT-CONTRACTS-2026-08-30.json`, hashes novamente conferidos antes da aplicação. Havia sete eventos; nenhum trigger em `dispatch_events`; os dois índices novos não existiam.
- Recuperação `JOURNEY-RECOVERY-2026-08-30.sql` ensaiada em PostgreSQL mínimo: hashes das três funções idênticos aos originais, permissões preservadas e histórico intacto. Não foi executada em produção.
- Migração publicada com `lock_timeout=3s` e `statement_timeout=20s`, sem alterar dados históricos, cargas, viagens ou integrações. O nome local foi alinhado à versão remota `20260830024309` para evitar publicação duplicada.
- Pós-deploy com `authenticated` e identidade do motorista atribuído: encerrou o turno atual após checklist novo, repetiu a mesma requisição sem duplicação, recusou checklist antigo no próximo início, abriu outro turno e validou pausa/retorno entre as viagens das cargas 1012 e 1003. Sete verificações negativas/de replay passaram, além das transições válidas.
- Rollback dos testes confirmado por consulta independente: sete eventos originais e última jornada `resume`. Viagem da 1003 continua `planned`/sem início; viagem da 1012 continua `in_transit` com o início anterior. Nenhum evento de teste persistiu.
- Tentativa com duas chamadas SQL não observou bloqueio entre elas (`second_trip_writer_serialized=false`), sem prova de sobreposição das sessões. Resultado **inconclusivo**, não aprovação de concorrência. Não confundir o uso de advisory lock e os testes sequenciais com ensaio concorrente completo.
- Frontend/regressão pós-aplicação: gate completo aprovado novamente com **710 testes/81 arquivos**, TypeScript, lint e build. Componentes renderizados cobrem estado pendente/erro, novo turno, fim em viagem concluída, retry, rascunho, conflito e isolamento das consultas. Isso não é E2E autenticado.
- Endpoint PostgREST usado pelo frontend: chamada sem sessão a `driver_get_journey_context` retornou HTTP 401 / SQLSTATE 42501, confirmando a RPC publicada e a negação ao acesso anônimo. Nenhuma chave foi exibida no log.
- Browser integrado indisponível por falha de inicialização do runtime. Alternativa gratuita `agent-browser@0.35.1`, com Chrome do sistema em perfil temporário isolado, abriu `/auth` no build local e renderizou formulário de login. Captura `journey-auth-smoke-2026-08-30.png`; nenhum login efetuado e nenhum perfil pessoal acessado. Este smoke não valida as telas autenticadas.
- Nenhum serviço pago, envio fiscal ou reintegração SSX foi ativado. Frontend e Edge Functions continuam sem publicação nesta etapa.

## Atualização — RPC legada restrita em 30/08/2026 03:33 UTC

- Achado confirmado: `driver_report_event_v1` aceitava `end_shift` como evento arbitrário, sem máquina de estados/checklist, e apenas verificava a identidade do motorista, sem estabelecer posse dos IDs de viagem/parada fornecidos. Alguns ramos antigos referenciam colunas inexistentes; isso não impedia a inserção de outros tipos de evento.
- Busca no frontend, Edge Functions e corpos das demais funções do banco não encontrou chamadores; restavam tipos gerados e testes/rascunho de hardening. Não se afirma ausência de clientes externos não inventariados.
- Reprodução sob `authenticated` no banco de produção, em transação revertida: encerramento arbitrário aceito. Consulta posterior confirmou zero eventos desse teste persistidos e jornada real ainda em `resume`.
- Contrato/ACL/hash capturados em `LEGACY-DRIVER-EVENT-PREDEPLOYMENT-2026-08-30.json`. Restrição e recuperação foram ensaiadas em PostgreSQL mínimo, com quatro testes. A recuperação explícita reabre o risco e não foi usada em produção.
- Migração `20260830033306_restrict_legacy_driver_event_api` revogou apenas execução de `PUBLIC`, `anon` e `authenticated`; `service_role` preservado. O hash do corpo ficou inalterado: `820fcce650445c44861f9cab01d39bc5`. Preflight aborta se o contrato tiver mudado.
- Pós-deploy: chamada legada negada com SQLSTATE 42501; retomada duplicada pela API atual negada com 23514; comunicação operacional e leitura de jornada pelas APIs atuais funcionaram. Todos os writes de QA foram revertidos; nenhuma alteração de início/status das viagens 1003/1012.
- Frontend pós-aplicação: gate completo **808/85**, incluindo componentes renderizados de jornada/entregas. Browser integrado falhou antes da conexão; alternativa gratuita em Chrome isolado abriu `https://agvlog.lovable.app/driver/journey`, redirecionou para `/auth`, exibiu labels Email/Senha/Entrar e não capturou erro de página. Screenshot `legacy-api-postdeploy-auth-2026-08-30.png` inspecionado visualmente; navegador fechado.
- Esse smoke é anônimo, **não E2E autenticado**. Foram solicitadas `E2E_DRIVER_EMAIL`/`E2E_DRIVER_PASSWORD` em `.env.test.local`, sem senha no chat; configuração ainda não disponível nesta etapa.
- Publicação identificada no Lovable (`agvlog.lovable.app`); o commit remoto diverge do checkout local. Nenhuma promoção da árvore local foi feita, pois ela inclui APIs de chegada/entrega ainda não publicadas e mudanças fiscais não concluídas. Não foi acionado agente pago do Lovable.
