import type {ExpenseComplementExtinction} from '@/lib/financial/expenseComplementExtinctionContract';
import {formatFinanceCents} from '@/lib/financial/ledgerContract';
export function ExpenseComplementExtinctionHistory({value}:{value:ExpenseComplementExtinction}){return <section aria-label="Histórico de extinção do complemento" className="rounded border border-amber-600 bg-amber-50 p-3 dark:bg-amber-950"><h4>Ajuste manual — complemento cancelado</h4><p>Nominal histórico cancelado: {formatFinanceCents(value.cancelled_nominal_cents)} · devido após esta extinção: R$ 0,00.</p><p>Saldo histórico sob responsabilidade do motorista: {formatFinanceCents(value.residual_cents)}. {value.residual_cents==='0'?'Não houve excedente nem nova dívida.':'Devoluções posteriores devem ser conferidas no histórico da responsabilidade.'}</p><p>Autor {value.actor_id} · {value.created_at}</p><p>Motivo: {value.reason}</p><p>Pedido {value.request_id} · extinção {value.id} · regularização {value.regularization_id}</p><p>Nenhum dinheiro foi movimentado ou liberado por esta correção.</p></section>;}
