import type {CashForecastSummary as Summary} from '@/lib/financial/cashForecastViewContract';
import {forecastMoney,forecastIssue} from '@/lib/financial/cashForecastViewLabels';
const confirmation={bank_confirmed:'Conferido por evidência bancária',cash_count:'Contagem de caixa',mixed_confirmed:'Banco e caixa conferidos',provisional:'Provisório — ainda exige conferência',unverified:'Sem comprovação suficiente'};
export function CashForecastSummary({data}:{data:Summary}){return <section aria-label="Resumo da previsão" className="space-y-3"><p>Empresa inteira · {data.counts.accounts} contas · base em {data.cutoff} · horizonte até {data.period_end}</p><p>Consulta capturada em {data.captured_at}. É a evidência disponível nesta consulta; não reconstrói o que era conhecido no passado.</p><p>Saldo-base: {forecastMoney(data.base.amount_cents)} · {confirmation[data.base.confirmation]}</p>{!data.projection_available&&<p role="alert">Projeção indisponível. A ausência de contas ou base válida não significa saldo zero.</p>}<div className="grid gap-3 md:grid-cols-2"><div className="rounded border p-3"><h3>Somente títulos confirmados</h3><p>Saldo previsto: {forecastMoney(data.confirmed?.closing_cents)}</p><p>{data.confirmed?.complete?'Fontes completas para este cenário.':'Cenário incompleto; confira as pendências.'}</p><p>Entradas previstas: {forecastMoney(data.scheduled?.confirmed_in_cents)} · saídas previstas: {forecastMoney(data.scheduled?.confirmed_out_cents)}</p></div><div className="rounded border p-3"><h3>Incluindo fretes a faturar</h3><p>Saldo previsto: {forecastMoney(data.expanded?.closing_cents)}</p><p>{data.expanded?.complete?'Fontes completas para este cenário.':'Cenário ampliado incompleto.'}</p><p>Expectativa adicional de fretes: {forecastMoney(data.scheduled?.unbilled_in_cents)}. Ainda não é título cobrado nem dinheiro recebido.</p></div></div><p>Movimentos registrados após a base: entradas {forecastMoney(data.recorded_totals?.in_cents)} · saídas {forecastMoney(data.recorded_totals?.out_cents)}. Registro de dinheiro não equivale a conciliação bancária.</p><p>Sem data válida no horizonte: títulos {forecastMoney(data.unscheduled?.confirmed_cents)} · fretes a faturar {forecastMoney(data.unscheduled?.unbilled_cents)}. Créditos sem título definido: {forecastMoney(data.unassigned_credit_cents)}.</p><p>Fontes integrais: {data.counts.origins} origens, {data.counts.movements} movimentos, {data.counts.credits} créditos. Versões não são somadas como novas receitas.</p><details><summary>Contas e evidências da base ({data.base.components.length})</summary>{data.base.components.map(c=><p key={c.account_id}>Conta {c.account_id} · {c.account_kind==='cash'?'Caixa':c.account_kind==='bank'?'Banco':'Tipo não suportado'} · {forecastMoney(c.amount_cents)} · {confirmation[c.confirmation]} · evidência {c.source_id||'não disponível'}</p>)}</details>{data.issue_groups.map(g=><p key={`${g.scope}:${g.code}`} role="alert">{forecastIssue(g.code)} ({g.count}) · {g.scope==='expanded'?'Cenário com fretes':g.scope==='confirmed'?'Títulos confirmados':'Ambos os cenários'} <span className="text-xs">{g.code}</span></p>)}</section>;}
