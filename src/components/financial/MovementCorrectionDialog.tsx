import {Dialog,DialogContent,DialogDescription,DialogHeader,DialogTitle} from '@/components/ui/dialog';
import {Button} from '@/components/ui/button';
import {MovementCorrectionReview} from './MovementCorrectionReview';
export function MovementCorrectionDialog({tenant,actor,movementId,onClose}:{tenant:string;actor:string;movementId:string;onClose:()=>void}){return <Dialog open onOpenChange={open=>{if(!open)onClose();}}><DialogContent className="max-h-[90vh] overflow-y-auto sm:max-w-3xl"><DialogHeader><DialogTitle>Corrigir ou excluir registro</DialogTitle><DialogDescription>Exclua um registro incorreto após conferir seus vínculos e informar o motivo. O original permanece no histórico; o extrato não é apagado e nenhuma transação bancária é executada.</DialogDescription></DialogHeader><MovementCorrectionReview key={`${tenant}:${actor}:${movementId}`} tenant={tenant} actor={actor} movementId={movementId}/><Button variant="outline" onClick={onClose}>Fechar conferência</Button></DialogContent></Dialog>;}
