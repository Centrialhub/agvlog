import {z} from 'zod';
import {receivableAgreementAllocationSchema} from './receivableAgreementContract';
const id=z.string().uuid();
export const receivableAdjustmentKindSchema=z.enum(['discount','loss']);
export const receivableAdjustmentCommandSchema=z.object({version:z.literal(1),tenant_id:id,request_id:id,receivable_id:id,action:z.enum(['apply','reverse']),kind:receivableAdjustmentKindSchema,adjustment_id:id.nullable(),amount_cents:z.string().regex(/^[1-9]\d{0,13}$/),effective_on:z.string().regex(/^\d{4}-\d{2}-\d{2}$/),expected_revision:z.string().regex(/^[a-f0-9]{32}$/),reason:z.string().refine(value=>value.trim().length>=5&&value.trim().length<=2000),installment_allocations:z.array(receivableAgreementAllocationSchema).min(1).max(100).optional(),expected_agreement_revision:z.string().regex(/^[a-f0-9]{32}$/).optional()}).strict().refine(value=>(value.action==='apply')===(value.adjustment_id===null)&&((value.installment_allocations===undefined)===(value.expected_agreement_revision===undefined))&&(value.action==='apply'||value.installment_allocations===undefined),'A reversão deve identificar o ajuste original e não aceita uma nova distribuição.');
export type ReceivableAdjustmentCommand=z.infer<typeof receivableAdjustmentCommandSchema>;
