import {supabase} from '@/integrations/supabase/client';
import {driverAdvancePositionsSchema,driverAdvanceReturnCommandSchema,driverAdvanceReturnOptionsSchema,driverAdvanceReturnPreviewSchema,type DriverAdvanceReturnCommand} from './driverAdvanceReturnContract';
type Rpc=(name:string,args:Record<string,unknown>)=>Promise<{data:unknown;error:unknown}>;const rpc=supabase.rpc.bind(supabase) as unknown as Rpc;
export async function readDriverAdvancePositions(tenant:string,actor:string,page=1){const {data,error}=await rpc('get_finance_driver_advance_positions',{_tenant_id:tenant,_page:page});if(error)throw error;const value=driverAdvancePositionsSchema.parse(data);if(value.tenant_id!==tenant||value.actor_id!==actor||value.page!==page)throw Error('A lista de adiantamentos não corresponde à empresa ou sessão atual.');return value;}
export async function readDriverAdvanceReturnOptions(tenant:string,actor:string,advance:string,search:string,page:number,expectedRevision:string|null){const {data,error}=await rpc('get_finance_driver_advance_return_options',{_tenant_id:tenant,_advance_movement_id:advance,_search:search,_page:page,_expected_revision:expectedRevision});if(error)throw error;const value=driverAdvanceReturnOptionsSchema.parse(data);if(value.tenant_id!==tenant||value.actor_id!==actor||value.advance_movement_id!==advance||value.search!==search||value.page!==page)throw Error('As entradas disponíveis não correspondem à empresa, sessão ou adiantamento atual.');return value;}
export async function readDriverAdvanceReturnPreview(tenant:string,actor:string,advance:string,incoming:string,amount:string){const {data,error}=await rpc('preview_finance_driver_advance_return',{_tenant_id:tenant,_advance_movement_id:advance,_incoming_movement_id:incoming,_amount_cents:amount});if(error)throw error;const value=driverAdvanceReturnPreviewSchema.parse(data);if(value.tenant_id!==tenant||value.actor_id!==actor||value.advance_movement_id!==advance||value.incoming_movement_id!==incoming||value.amount_cents!==amount)throw Error('A conferência não corresponde à empresa, sessão ou devolução solicitada.');return value;}
export const sendDriverAdvanceReturn=(payload:DriverAdvanceReturnCommand)=>rpc('record_finance_driver_advance_return',{_payload:driverAdvanceReturnCommandSchema.parse(payload)});
