import type {PayableApprovalCommand,PayableApprovalPreview} from '@/lib/financial/payableApprovalContract';
export const approvalIds={tenant:crypto.randomUUID(),actor:crypto.randomUUID(),payable:crypto.randomUUID()};
export const approvalPreview:PayableApprovalPreview={version:1,tenant_id:approvalIds.tenant,actor_id:approvalIds.actor,payable_id:approvalIds.payable,revision:'a'.repeat(32),obligation:{amount_cents:'5000',paid_cents:'0',status:'pending',beneficiary_id:crypto.randomUUID(),beneficiary_name:'Fornecedor conferido',source_table:null,source_id:null},cost_origin:null,blockers:[],eligible:true,can_approve:true,can_execute:true};
export const approvalInput={payableId:approvalIds.payable,amountCents:'5000',revision:approvalPreview.revision,reason:'Valor e documentos conferidos',expected:{amount_cents:'5000',cost_revision:null}};
export function approvalSuccess(c:PayableApprovalCommand){return {data:{version:1,tenant_id:c.tenant_id,actor_id:approvalIds.actor,payable_id:c.payable_id,request_id:c.request_id,approval_event_id:crypto.randomUUID(),confirmed:true,amount_cents:c.amount_cents,status:'approved',approved_at:'2026-09-11T10:00:00Z',cost_revision:null,cash_changed:false},error:null};}
