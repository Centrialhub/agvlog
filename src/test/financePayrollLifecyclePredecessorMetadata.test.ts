// @vitest-environment node
import {it} from 'vitest';import {writeFileSync} from 'node:fs';import {createFinanceForwardBlockDatabase} from './helpers/financeForwardBlockDatabase';
it('captures source metadata of complete access-wrapped payroll predecessors',async()=>{const {db}=await createFinanceForwardBlockDatabase('20260910133352');try{const rows=(await db.query("select oid::regprocedure::text signature,md5(replace(prosrc,E'\\r\\n',E'\\n')) hash,prosecdef,provolatile,proconfig,has_function_privilege('authenticated',oid,'execute') authenticated,has_function_privilege('anon',oid,'execute') anon,has_function_privilege('service_role',oid,'execute') service from pg_proc where pronamespace='public'::regnamespace and proname in('generate_payroll_period','approve_payroll_period','close_payroll_period','recompute_payroll_entry_totals','recalculate_payroll_entry','add_payroll_manual_item','delete_payroll_entry_item') order by proname")).rows;writeFileSync('docs/qa/finance-payroll-lifecycle-predecessor-metadata-2026-09-10.json',JSON.stringify(rows,null,2));}finally{await db.close();}},120000);
