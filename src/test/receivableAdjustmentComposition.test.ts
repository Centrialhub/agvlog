import {describe,it,expect} from 'vitest';
import {creditCompositionValid,receivableListSettlement} from '@/lib/financial/receivableCreditAmounts';
import {receivableAdjustmentCompositionSchema,adjustmentCompositionValid} from '@/lib/financial/receivableAdjustmentContract';
describe('composição com baixas sem caixa',()=>{
 it('preserva dinheiro900 e nominal1000 ao quitar desconto100',()=>{const row={cash_received_cents:'90000',credit_applied_cents:'0',discount_cents:'10000',loss_cents:'0',adjustment_cents:'10000',settled_cents:'100000'};expect(creditCompositionValid(row,'100000')).toBe(true);expect(receivableListSettlement({...row,amount:1000,received_amount:1000,status:'received',open_cents:'0'})).toMatchObject({nominal:'100000',cash:'90000',discount:'10000',loss:'0',open:'0'});expect(adjustmentCompositionValid(receivableAdjustmentCompositionSchema.parse({...row,amount_cents:'100000',open_cents:'0'}))).toBe(true);});
 it('preserva versão anterior e rejeita composição parcial ou equação inventada',()=>{const old={cash_received_cents:900,credit_applied_cents:100,settled_cents:1000};expect(creditCompositionValid(old)).toBe(true);expect(creditCompositionValid({...old,discount_cents:0})).toBe(false);expect(creditCompositionValid({...old,discount_cents:100,loss_cents:0,adjustment_cents:100})).toBe(false);expect(creditCompositionValid({...old,discount_cents:null,loss_cents:null,adjustment_cents:null})).toBe(false);expect(creditCompositionValid({cash_received_cents:null,credit_applied_cents:null,settled_cents:null,discount_cents:null,loss_cents:null,adjustment_cents:null})).toBe(true);});
});
