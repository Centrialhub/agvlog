import {proposedAppliedCents} from '@/lib/financial/unloadingCostRegularizationContract';
import {render,screen,fireEvent,cleanup} from '@testing-library/react';
import {afterEach,it,expect,vi} from 'vitest';
import {UnloadingCostDispositionEditor} from '@/components/financial/UnloadingCostDispositionEditor';
afterEach(cleanup);
it('requires an explicit applied value and shows recovery separately from the unchanged reserved money',()=>{const onChange=vi.fn(),source={kind:'payable_link' as const,id:crypto.randomUUID(),movementId:crypto.randomUUID(),grossReservedCents:'15000',responsibleId:crypto.randomUUID(),responsibleName:'Prestador que recebeu',dispositionType:'payment_recovery' as const,issue:null};const view=render(<UnloadingCostDispositionEditor rows={[{source,applied:''}]} onChange={onChange}/>);expect(screen.getByLabelText('Valor aplicado da fonte 1')).toHaveValue('');fireEvent.change(screen.getByLabelText('Valor aplicado da fonte 1'),{target:{value:'120,00'}});expect(onChange).toHaveBeenCalledWith(0,'120,00');view.rerender(<UnloadingCostDispositionEditor rows={[{source,applied:'120,00'}]} onChange={onChange}/>);expect(screen.getByText('Valor reservado original: R$ 150,00')).toBeInTheDocument();expect(screen.getByText('Valor a recuperar de quem recebeu: R$ 30,00')).toBeInTheDocument();expect(screen.queryByRole('button')).not.toBeInTheDocument();});
it('parses exact cents without rounding fractional cents or accepting negative values',()=>{expect(proposedAppliedCents('120,01')).toBe('12001');expect(proposedAppliedCents('-1')).toBeNull();expect(proposedAppliedCents('0,001')).toBeNull();expect(proposedAppliedCents('0')).toBe('0');});
