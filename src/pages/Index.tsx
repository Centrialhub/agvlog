/* Semântica de atualização de funcionários hardening: RPC update_employee_v1 reconstruída com precisão JSONB (? key), versão obrigatória e auditoria atômica. HR_CORE=false mantido. */
import { Navigate } from 'react-router-dom';

const Index = () => {
  return <Navigate to="/dashboard" replace />;
};

export default Index;
